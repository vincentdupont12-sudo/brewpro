/** @type {import('next').NextConfig} */
const nextConfig = {
  /* Tes autres options si tu en as (ex: images) */
  
  // Si tu veux ignorer les erreurs au build pour que tes potes puissent voir l'app :
  typescript: {
    ignoreBuildErrors: true,
  },
};

export default nextConfig;